hazelcast:
  #not setting the cluster-name here instead doing it via env var
  #cluster-name: hazelcast-cluster
  properties:
    hazelcast.logging.type: log4j2
  network:
    join:
      multicast:
        enabled: true
      kubernetes:
        enabled: false